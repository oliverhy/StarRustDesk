# StarRustDesk 1.1.0（1001004）

## 本次更新

- 修复 HarmonyOS PC 使用物理键盘时，Shift 配合数字行无法输入 `! @ # $ % ^ & * ( )` 的问题。
- 普通数字、Shift 大小写及中文输入保持原有行为，不受本次修复影响。
- `Ctrl`、`Alt` 或 `Win` 配合 Shift 与数字键的快捷键继续按物理按键传递，避免破坏远端快捷操作。
- 完善数字行长按重复、按键释放和会话重置处理，降低 Shift 或数字键状态残留的风险。

## 下载说明

Release 仅提供 **Release 构建的未签名 HAP**，需要使用自己的有效证书签名后安装。正式签名的 AppGallery `.app` 不上传 GitHub。

## 验证范围

- Shift 数字行符号映射回归检查通过。
- 原有输入事件队列回归检查通过。
- Release 未签名 HAP 已重新构建，包内版本为 `1.1.0（1001004）`，SHA-256 为 `38D13EEA34187E394FB851CB30775D2609595A134BDC19070A853E819CD1126C`。
- 正式 AppGallery `.app` 的外层签名、包内 HAP 签名及原生代码签名均验证通过，SHA-256 为 `DDC8C64D8BC56B4795A537F7BA3CE4674B7CDF5BF03F26663D2CEF01F4339052`。
- 以上自动检查不代替不同键盘布局和远端操作系统下的真实输入验收。
