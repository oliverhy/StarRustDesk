# StarRustDesk 1.2.0（1002000）

## 新增与优化

- 新增华为个人云空间同步，可在同一华为账号的设备间同步已保存连接、分组和服务器配置。
- 密码与服务器密钥默认不上传；用户可选择使用 AES-256-GCM 加密后同步敏感信息，并支持手动同步和云端恢复。
- 完善官方服务器与自建服务器连接流程，补充 IPv4/IPv6、TCP、中继、UDP/KCP 与 WebRTC 连接能力及诊断信息。
- 优化连接建立、打洞和中继回退速度，修复候选地址选择、连接重试及快速重连中的稳定性问题。
- 修复多屏远控时画面与远端光标状态可能串屏、跳动或显示不一致的问题。
- 自建服务器配置保存后立即生效，支持配置导入与导出；连接密码弹窗可直接输入，并可选择是否保存。
- 优化长按拖动判定，降低鼠标左键长按和触摸操作的误触发。
- 更新连接页紧凑布局、排序操作和连接信息面板；首页展开折叠及“连接 / 设置”切换增加平滑过渡动画。

更新后首次启动将显示本次更新内容，设置中的更新日志同步更新，最新版本位于最上方。

## 下载说明

- 本 Release 仅提供 Release 模式构建的未签名 HAP，需要自行签名后安装。
- 华为应用市场使用的正式签名 APP 包只保存在本地，不上传 GitHub。
- 网络协议和云空间能力受服务器、账号、系统版本及网络环境影响，仍需在实际环境中验证。

## 构建验证

- 16 个 JavaScript 回归测试脚本全部通过。
- Rust 核心测试 `33 passed; 0 failed`，ARM64 与 x86_64 Release 静态库构建通过。
- Release APP 构建通过，包内版本为 `1.2.0（1002000）`，`debug=false`。
- 外层 APP、内层 HAP、正式 Release Profile 和摘要验证通过，内层 HAP 字节一致。
- 正式 APP SHA-256：`5B3B98627F8AA0955B2391812E5923FC92E3EBFE88EBF49A77BE705C6FE3672F`
- 未签名 HAP SHA-256：`8C38B4D513D26B92A1EB89F1CCEC5E70D0639EB97276CED3F26488EB7637F7C2`
