# StarRustDesk 1.2.16（1002016）

## 更新内容

1. 连接失败错误码提示改为中文，并明确区分 ID 服务器密钥不匹配与服务器密钥无效。
2. 远端密码错误、权限拒绝、连接超时与连接中断等常见提示同步中文化。
3. 保持自动重连和由用户确认的降级连接流程；更新对应回归检查。

## 下载与验证

GitHub Release 仅提供**未签名 HAP**，须自行签名后安装。正式签名的 AppGallery `.app` 仅保存在本地用于上架，不上传 GitHub。

- 包名：`com.yinhe.starrustdesk`；版本：`1.2.16（1002016）`。
- AppGallery 正式签名 `.app` SHA-256：`576CB19C4F65FFC6F25C4E620BCAEBB686E17E9ED7492E9D7BBE1BABEAEDE85B`。外层 APP、内层 HAP 签名和发布描述文件验证通过；配置为 `release` / `app_gallery`，包内 `debug=false`，包含 ARM64 和 x86_64 原生库。
- GitHub Release 未签名 `.hap` SHA-256：`E6C62B0F1ADD83FFD892AD0387F4F80630DD2555A6350B0B4A3028DA2B202631`；与 Release 构建生成的未签名 HAP 字节一致。
- 39 组脚本回归、53 项 Rust 核心测试通过；Release `assembleApp` 构建通过。
- 自动测试、构建和签名验证不等于应用市场审核通过。
